apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gavel
  namespace: gavel
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - gavel.example.com
      secretName: gavel-tls
  rules:
    - host: gavel.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx-demo
                port:
                  number: 80
